server {
    server_name enter.turkmendili.ru;

    server_tokens off;
    proxy_hide_header X-Powered-By;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    location = / {
        return 301 /dashboard/;
    }

    location = /dashboard {
        return 301 /dashboard/;
    }

    # SPA dashboard (alias avoids /dashboard/index.html mismatch)
    location /dashboard/ {
        alias /opt/xpert/app/dashboard/build/;
        index index.html;
        try_files  / /index.html;
    }

    # Static assets
    location /dashboard/assets/ {
        alias /opt/xpert/app/dashboard/build/assets/;
        expires 1y;
        add_header Cache-Control public, immutable;
        access_log off;
    }

    location /dashboard/statics/ {
        alias /opt/xpert/app/dashboard/build/statics/;
        expires 1y;
        add_header Cache-Control public, immutable;
        access_log off;
    }

    location = /dashboard/favicon.ico {
        alias /opt/xpert/app/dashboard/build/statics/favicon/favicon.ico;
        expires 1y;
        add_header Cache-Control public, immutable;
        access_log off;
    }

    location = /dashboard/site.webmanifest {
        alias /opt/xpert/app/dashboard/build/statics/favicon/site.webmanifest;
        expires 1y;
        add_header Cache-Control public, immutable;
        access_log off;
    }

    # API
    # Block install API on main panel domain
    location ^~ /api/install/ {
        return 404;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host ;
        proxy_set_header X-Real-IP ;
        proxy_set_header X-Forwarded-For ;
        proxy_set_header X-Forwarded-Proto ;
        proxy_redirect off;

        proxy_http_version 1.1;
        proxy_set_header Upgrade ;
        proxy_set_header Connection upgrade;

        proxy_connect_timeout 60s;
        proxy_send_timeout 900s;
        proxy_read_timeout 900s;
    }

    # Exec proxy for subscription
    location /exec {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host ;
        proxy_set_header X-Real-IP ;
        proxy_set_header X-Forwarded-For ;
        proxy_set_header X-Forwarded-Proto ;
        proxy_redirect off;
        proxy_http_version 1.1;
        proxy_set_header Connection ;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Subscription (/sub) with rate/conn limits + real client IP forwarding
    location ^~ /sub/ {
        limit_req zone=xpert_sub_per_ip burst=10 nodelay;
        limit_conn xpert_sub_conn_per_ip 10;

        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location ~ ^/(sub|admin|user|node|system|core)/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host ;
        proxy_set_header X-Real-IP ;
        proxy_set_header X-Forwarded-For ;
        proxy_set_header X-Forwarded-Proto ;
        proxy_redirect off;

        proxy_http_version 1.1;
        proxy_set_header Upgrade ;
        proxy_set_header Connection upgrade;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    listen 443 ssl http2;
    ssl_certificate /etc/letsencrypt/live/enter.turkmendili.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/enter.turkmendili.ru/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_buffer_size 8k;
    ssl_stapling on;
    ssl_stapling_verify on;
}

# Install-only domain (OTP downloads)
server {
    server_name xpert.mediatmshow.online;

    server_tokens off;
    proxy_hide_header X-Powered-By;

    location / {
        return 404;
    }

    location ^~ /api/install/ {
        include /etc/nginx/xpert_install_gate.conf;

        limit_req zone=req_per_ip burst=10 nodelay;
        limit_conn conn_per_ip 10;

        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection upgrade;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    listen 443 ssl http2;
    ssl_certificate /etc/letsencrypt/live/xpert.mediatmshow.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/xpert.mediatmshow.online/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_buffer_size 8k;
    ssl_stapling on;
    ssl_stapling_verify on;
}

server {
    listen 80;
    server_name xpert.mediatmshow.online;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type "text/plain";
    }
    return 301 https://$host$request_uri;
}

server {
    if ( = enter.turkmendili.ru) {
        return 301 https://;
    }

    listen 80;
    server_name enter.turkmendili.ru;
    return 404;
}

# esen64.ru — subscription proxy for admin turkmen (port 2161)
server {
    server_name esen64.ru;

    server_tokens off;
    proxy_hide_header X-Powered-By;

    location ^~ /sub/ {
        limit_req zone=xpert_sub_per_ip burst=10 nodelay;
        limit_conn xpert_sub_conn_per_ip 10;

        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location / {
        return 404;
    }

    listen 2161 ssl http2;
    ssl_certificate /etc/letsencrypt/live/esen64.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/esen64.ru/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}
